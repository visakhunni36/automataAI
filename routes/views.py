from . import routes
from controllers  import *

from flask import Flask, request, jsonify, Response, redirect, url_for, session, render_template
import json
from flask_cors import CORS, cross_origin


@routes.route("/dashboard")
def dashboard():
	pass










































































































































